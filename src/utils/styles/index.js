import {
  default as styled,
  css,
  keyframes,
  ThemeProvider,
} from 'styled-components'
import GlobalStyles from './GlobalStyles'
import { device, size } from './device'

export { css, keyframes, device, size, ThemeProvider, GlobalStyles }
export default styled
